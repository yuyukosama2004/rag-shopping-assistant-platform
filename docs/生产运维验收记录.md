# 生产运维验收记录

## 2026-07-15 阶段 6 实机验收

验收环境为目标 Linux 服务器上的商家单实例部署。所有检查均使用仓库内脚本和固定版本容器执行；敏感配置、备份内容和日志正文未写入本记录。

| 验收项 | 结果 | 证据摘要 |
| --- | --- | --- |
| 版本化升级 | 通过 | `upgrade` 完成后四个应用容器均为 `healthy`，网关业务请求成功，活动版本已切换 |
| 前端容器构建 | 通过 | Node 22 容器完成类型检查和 Vite 构建，依赖审计为 0 漏洞，`dist` 与 `node_modules` 均归宿主部署用户所有 |
| 监控栈 | 通过 | Prometheus、Alertmanager、Node Exporter、cAdvisor 均就绪；四个应用加四个监控目标共 8 个 target 为 `UP` |
| 应用健康指标 | 通过 | 四个 `biyesheji_application_health` 序列均为 `1`，8 条告警规则成功加载 |
| 容器自动恢复 | 通过 | 向用户服务 JVM PID 1 发送 `SIGTERM` 后，`RestartCount` 增加并重新达到 `healthy` |
| 告警生命周期 | 通过 | 停止用户服务超过 2 分钟后 `BiyeshejiMetricsTargetDown` 进入 firing，Alertmanager 收到告警；服务恢复后规则转为 resolved |
| 结构化日志 | 通过 | 四服务抽样日志全部可解析为 ECS JSON，包含时间、消息、`log.level` 和 `service.name` |
| 加密备份与恢复 | 通过 | 数据库与媒体加密备份成功，并已在隔离恢复流程中完成校验与业务抽查 |
| 版本回滚 | 通过 | 已完成上一版本激活、健康检查和当前版本恢复演练 |
| 公网 HTTPS | 待现场验收 | TLS、Let's Encrypt、HSTS、安全头和入口限流配置已完成自动化校验；等待真实域名与公网 DNS 后签发证书并验证仅开放 80/443 |

## 演练边界

- `docker stop` 和 `docker kill` 属于人工停止，Docker 不会用 `unless-stopped` 将其视作应用自行崩溃；自动恢复演练使用容器内 `SIGTERM`。
- 停止整个容器会先触发采集目标不可达告警；依赖异常但应用仍运行时才会触发聚合健康告警。
- Alertmanager 当前默认 receiver 仅保留本地告警。商家正式运营前必须配置自己的外部通知渠道，并将凭据放在受限文件或 secret 中。
