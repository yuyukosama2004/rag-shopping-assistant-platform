# 商家自托管 AI 商城分阶段开发计划

> 文档状态：后续开发主路线图  
> 制定日期：2026-07-13  
> 产品定位：单商家、单店铺、单实例、自托管的 AI 导购商城  
> 当前基线：单商家后台、商品/SKU/库存、线下支付履约、退款记录、可运营 AI 导购和生产运维能力已部署；阶段 6 只待真实域名的公网 TLS 验收，阶段 7 的系统验收与商家试点仍待完成。

## 1. 产品目标

本项目不建设多商家平台，也不承担平台运营、商家入驻和平台审核职能。每位商家拥有并部署一套独立系统，自行管理店铺、商品、库存、订单、发货、售后和 AI 配置。

最终产品由两个入口组成：

1. **消费者商城**：浏览商品、搜索筛选、AI 咨询、购物车、下单、查询订单和售后状态。
2. **商家工作台**：配置店铺、管理商品与 SKU、调整库存、处理订单、录入发货信息、查看经营数据和维护 AI 知识。

### 1.1 核心成功标准

一个没有开发能力的商家，应能够按照文档完成部署，并独立完成以下闭环：

```text
初始化店主账号
  → 设置店铺信息
  → 创建商品、SKU、价格和库存
  → 上架商品
  → 消费者浏览、咨询并下单
  → 商家确认收款/处理订单
  → 商家填写物流信息并发货
  → 消费者确认完成
  → 商家备份数据并安全升级
```

### 1.2 明确不做的范围

以下能力不属于首个可用版本，除非后续重新立项：

- 多商家入驻、平台抽佣、平台管理员和商家审核。
- 同一部署承载多个店铺的多租户系统。
- 平台级资金清算、担保交易和复杂分账。
- 自建物流网络、仓储管理系统和财务 ERP。
- 多国家、多币种、多税率和跨境合规。
- 直播、拼团、分销、会员等级等大型营销体系。

## 2. 当前基线与主要差距

| 领域 | 当前能力 | 生产目标 | 差距 |
| --- | --- | --- | --- |
| 消费者前端 | 首页、商品、购物车、结算、订单、账户、AI 导购 | 可稳定完成购买和订单查询 | 需要端到端测试、错误体验和移动端验收 |
| 商品 | 面向消费者的只读查询；颜色/容量为 JSON 选项 | 商家可维护商品、媒体、SKU、价格、库存和上下架 | 缺商家写接口、真正 SKU 模型和后台页面 |
| 库存 | 按商品库存，具备预留、确认、释放逻辑 | 按 SKU 管理，可盘点、调整并追溯 | 缺库存流水、SKU 维度和商家调整入口 |
| 订单 | 消费者下单、模拟支付、取消 | 商家接单、确认收款、发货、关闭和售后 | 缺商家视角、履约状态、物流和操作记录 |
| 权限 | 消费者账户和 JWT | 店主/店员/消费者分权 | 缺角色、权限和首次初始化机制 |
| 店铺 | 固定 PhoneMall 品牌 | 商家可配置名称、Logo、联系方式和规则 | 缺店铺配置模型和动态展示 |
| AI | 商品 RAG、SSE 对话、商品描述补全基础 | 随商品上下架自动同步，成本和内容可控 | 缺索引任务状态、知识管理和运营配置 |
| 部署运维 | Docker 基础设施、构建脚本、Nginx、CI | 一条命令安装/升级/备份/恢复 | 应用编排、健康检查、TLS、监控和回滚仍需完善 |

## 3. 总体设计约束

### 3.1 单店单实例

- 每套数据库只服务一个商家，因此核心业务表暂不增加 `tenant_id`。
- 店铺资料使用单行配置表；系统启动时必须存在且只能存在一份有效店铺配置。
- 商家之间通过独立部署、独立数据库、独立域名和独立密钥隔离。
- 备份、升级和故障恢复以单实例为单位执行。

### 3.2 角色与权限

首版采用三个角色：

| 角色 | 权限 |
| --- | --- |
| `OWNER` | 店铺全部配置、员工管理、商品、库存、订单、AI 和系统设置 |
| `STAFF` | 商品、库存、订单处理；不可修改店主账号、系统密钥和危险设置 |
| `CUSTOMER` | 消费者商城、地址、购物车、本人订单和 AI 导购 |

约束：

- 首个 `OWNER` 通过一次性初始化命令或一次性令牌创建，禁止默认用户名和默认密码。
- 消费者注册只能获得 `CUSTOMER`，不能通过请求参数提升角色。
- `/api/merchant/**` 必须由网关鉴权，下游服务仍需执行角色和资源级校验。
- 所有商家写操作记录审计日志，包括操作者、对象、动作、结果和时间。

### 3.3 技术演进原则

- 继续使用现有 Spring Cloud 服务和 Vue 前端，首版不进行大规模重写。
- 商家工作台先集成在现有 Vue 应用的 `/merchant` 路由下，采用独立布局和路由守卫。
- 商品与 SKU 归 `product-service`；订单、库存预留和履约归 `order-service`；身份与店铺账号归 `user-service`。
- 对外消费者接口保持兼容；商家接口统一使用 `/api/merchant/**`。
- 所有数据库结构变化必须使用 Flyway，禁止在部署脚本中临时执行 `ALTER TABLE`。
- 先实现货到付款/线下收款的完整履约，再接真实支付，避免模拟支付进入生产。

## 4. 目标数据模型

以下为目标模型，具体字段通过阶段设计评审确定。

| 表/聚合 | 目的 | 关键约束 |
| --- | --- | --- |
| `t_store_setting` | 店名、Logo、联系方式、地址、营业状态、配送与售后说明 | 单实例仅一条有效配置 |
| `t_user` | 店主、店员和消费者账户 | 增加角色与账号状态；角色不可由普通资料接口修改 |
| `t_product` | SPU 级商品内容 | 草稿、上架、下架；软删除后消费者不可见 |
| `t_product_sku` | 规格组合、售价、划线价、商家编码 | SKU 编码唯一；至少一个可售 SKU 才能上架 |
| `t_product_media` | 主图、轮播图、详情媒体及排序 | 删除商品时处理媒体引用 |
| `t_stock` | SKU 总库存、锁定库存、可用库存、版本号 | `total = locked + available` 且均非负 |
| `t_stock_ledger` | 入库、扣减、释放、盘点和人工调整流水 | 每次变更具有业务来源和操作人 |
| `t_order` | 消费者订单和履约状态 | 状态转换受状态机约束 |
| `t_order_item` | 商品、SKU、价格和规格快照 | 下单后不随商品修改而改变 |
| `t_shipment` | 承运商、运单号、发货时间和备注 | 仅已确认订单可发货 |
| `t_shipping_rule` | 配送方式、基础运费、包邮门槛和自提配置 | 下单时保存规则与运费快照 |
| `t_refund_record` | 手工或渠道退款的金额、原因和处理状态 | 退款金额不得超过实付金额 |
| `t_order_operation` | 商家和系统的订单操作记录 | 只追加，不覆盖历史 |
| `t_audit_log` | 商家后台关键操作审计 | 不记录密码、Token、完整地址等敏感内容 |
| `t_ai_index_task` | 商品/知识的向量索引任务 | 可重试、可观察、可防止旧版本覆盖新版本 |

## 5. 分阶段实施路线

阶段必须按顺序通过验收门槛。若前一阶段核心验收失败，不开始依赖它的下一阶段。

### 阶段 0：范围冻结与可重复基线

**目标**：把当前可运行版本变成后续开发的可信起点，并冻结首版业务范围。

任务：

- [x] 在 README 中更新产品定位，删除“多商家平台”暗示。
- [x] 记录当前部署版本、服务端口、环境变量和数据库迁移版本。
- [x] 将四个应用服务纳入统一 Compose，支持可配置网关宿主机端口。
- [x] 为所有容器增加 restart policy、健康检查、日志轮转和资源限制。
- [x] 增加 `dev`、`test`、`prod` 配置分层和完整 `.env.example`。
- [x] 建立 ADR，记录单店单实例、角色设计、SKU 迁移和支付策略。
- [x] 为消费者核心流程建立最小冒烟测试基线。

交付物：

- 可重复的一键启动与停止命令。
- 产品范围说明、架构决策记录和冒烟测试报告。
- 干净数据库与旧数据库升级均可启动的迁移验证。

验收门槛：

- 在全新机器上仅依据 README 可完成部署。
- 连续执行两次启动/升级命令结果幂等。
- 首页、商品接口、登录和订单基础接口通过冒烟测试。
- CI 同时验证后端、前端、Compose 和数据库迁移。

### 阶段 1：店主身份、权限与店铺配置

**目标**：建立安全的商家入口，让系统知道“谁能管理这家店”。

后端任务：

- [x] 为用户增加 `OWNER/STAFF/CUSTOMER` 角色与启用状态。
- [x] 提供一次性店主初始化令牌；初始化成功后接口永久拒绝再次初始化。
- [x] 增加店员创建、禁用、重置凭据和最小权限接口。
- [x] 增加店铺配置查询与修改接口。
- [x] 网关保护 `/api/merchant/**`，并增加服务层角色校验。
- [x] 增加商家登录限流、失败锁定和关键操作审计。

前端任务：

- [x] 新增 `/merchant/login`、商家布局和菜单；无权限访问会返回消费者商城。
- [x] 新增店铺设置页面和店员管理页面。
- [x] 消费者页面动态读取店名、Logo、联系方式和营业状态。

验收门槛：

- [x] 消费者无法访问任何商家 API 或页面数据。
- [x] 店员不能修改店主、系统密钥和受限设置。
- [x] 不存在默认店主密码；普通注册无法创建商家角色。
- [x] 修改店铺信息后消费者首页刷新即可生效。

### 阶段 2：商品、SKU、图片与库存管理

**目标**：商家可以自行选择并维护真实销售商品。

后端任务：

- [x] 将颜色/容量 JSON 选项迁移为真实 `Product + SKU` 模型。
- [x] 将库存迁移到 SKU 维度，并保留旧商品数据的可回滚迁移脚本。
- [x] 增加商品草稿、编辑、复制、上下架、软删除和批量操作接口。
- [x] 增加品牌、分类和商家自定义属性维护，避免继续依赖代码或固定演示数据。
- [x] 增加 SKU 编码、售价、划线价、规格组合和启停接口。
- [x] 增加库存入库、盘点、人工调整和库存流水接口。
- [x] 增加图片上传、排序、删除和类型/大小校验；首版支持本地持久卷，并预留 S3 兼容配置。
- [x] 提供带校验报告的 CSV 商品/SKU 批量导入与导出，失败行不得产生半成品数据。
- [x] 商品、SKU 或库存变更时精确失效缓存。

前端任务：

- [x] 新增商品列表、创建/编辑、SKU 编辑、媒体管理和上下架页面。
- [x] 新增库存列表、调整对话框和库存流水页面。
- [x] 表单提供草稿保存、离开提醒、字段校验和明确错误反馈。
- [x] 消费者详情页按 SKU 组合展示价格与可售库存。

验收门槛：

- 店主可从零创建带图片和多个 SKU 的商品并上架。
- 上架商品在消费者端可见，下架后不可购买且缓存同步失效。
- 任意库存变更均能追溯到流水，库存不变量始终成立。
- 旧商品和历史订单升级后仍可读取，迁移具备备份和回滚说明。

### 阶段 3：订单履约与商家订单工作台

**目标**：完成从消费者下单到商家发货、消费者收货的闭环。

建议首版状态机：

```text
PENDING_PAYMENT
  ├─→ CANCELLED
  └─→ PAID
        ├─→ CANCELLED/REFUND_PENDING（按支付方式处理）
        └─→ PROCESSING
              └─→ SHIPPED
                    └─→ COMPLETED
```

后端任务：

- [x] 使用枚举和显式状态机替代散落的数字状态判断。
- [x] 订单项保存商品、SKU、规格、单价、主图和名称快照。
- [x] 增加商家订单分页、详情、备注、确认收款、接单、发货和关闭接口。
- [x] 增加承运商、运单号、发货时间和操作记录。
- [x] 增加基础配送规则：固定运费、满额包邮、自提和不可配送提示；订单保存运费快照。
- [x] 增加手工退款/退款备注记录；未接真实支付前不得伪装成渠道已退款。
- [x] 明确取消、超时、重复请求和服务重启时的库存补偿规则。
- [x] 所有状态变更使用幂等键或乐观锁，禁止重复发货和重复释放库存。
- [x] 首版实现 `COD/OFFLINE` 支付方式；生产环境移除消费者“模拟支付”入口。
- [x] 提供最小经营概览：待处理订单、低库存 SKU、当日订单数和已确认销售额，并明确统计口径。

前端任务：

- [x] 新增商家订单列表、筛选、详情、备注、确认收款和发货页面。
- [x] 消费者订单页显示付款、处理、发货和完成时间线。
- [x] 明确展示线下付款/货到付款说明，避免误导为在线支付成功。

验收门槛：

- 完整跑通创建订单、确认收款、发货、完成和取消流程。
- 运费、商品小计和订单总额由服务端计算且在订单中保存快照。
- 重复点击、请求超时、服务重启不会造成重复扣减或负库存。
- 商家看不到密码等敏感字段；消费者只能读取本人订单。
- 每次订单状态变化都有操作记录且前后端状态一致。

### 阶段 4：消费者体验与通知

**目标**：使消费者端达到可公开试用的完整度。

任务：

- [x] 统一 Axios/SSE 鉴权、刷新令牌、超时和错误处理；AI 不再绕过统一认证逻辑。
- [x] 完善搜索、筛选、空状态、售罄、下架、网络错误和重复提交体验。
- [x] 完成移动端主要断点适配，并在 Chromium 桌面与手机视口执行全栈验收。
- [x] 增加订单创建、取消、确认收款和发货通知；首版支持商家通过部署环境配置带签名的可靠 Webhook。
- [x] 增加店铺配送、隐私、售后和联系方式页面。
- [x] 增加消费者账户资料导出和注销流程；注销不得破坏依法或履约所需的订单记录。
- [x] 对消费者可见状态和商家内部状态建立稳定映射。

验收门槛：

- 关键页面在手机和桌面浏览器均可操作，无阻塞性控制台错误。
- Token 过期可自动恢复，恢复失败会安全退出且不丢失当前操作提示。
- 商品下架、库存不足、重复下单等失败均有清晰反馈。
- 消费者能获取订单状态变化通知或明确的站内状态更新。

### 阶段 5：AI 导购与商家知识管理

**目标**：AI 只推荐当前商家真实可售商品，并可被商家安全控制。

任务：

- [x] 商品上架、修改、下架和删除触发可追踪的向量索引任务。
- [x] 检索结果过滤下架 SKU、无库存商品和过期索引版本。
- [x] 新增商家 AI 设置：启停、模型、温度、每日预算、单用户额度和免责声明。
- [x] 新增 FAQ、配送、售后和店铺知识维护页面。
- [x] 增加提示注入防护、输出长度限制、敏感内容过滤和超时降级。
- [x] 记录请求量、Token/费用估算、失败率和索引状态，不记录不必要的个人信息。
- [x] AI 不确定时引导联系商家，不编造价格、库存、优惠和售后承诺。

完成记录（2026-07-15）：

- PR #22–#24 完成实时可售过滤与防编造约束；PR #25–#29 完成设置、知识、索引、预算、指标和流式敏感词过滤。
- GitHub Actions 后端、前端和配置检查全部通过；服务器 Java 17 完整构建通过，order-service 17 项测试通过。
- 服务器已迁移至 Flyway V15，4 个应用服务健康，网关、商品 API 和前端入口冒烟通过。
- 浏览器自动化工具受私有 LAN URL 策略限制，阶段 4 的桌面/移动端真实浏览器交互验收仍保持未完成。

验收门槛：

- AI 推荐的商品均为当前已上架且可售商品，链接可打开。
- 商品下架后在约定时间内从检索结果移除。
- 未配置密钥或额度耗尽时，商城其他功能正常且提示清楚。
- 商家可查看索引失败任务并安全重试。

### 阶段 6：生产部署、安全与运维

**目标**：商家可以长期维护自己的实例，而不是依赖开发者手工救场。

任务：

- [x] 提供完整应用 Compose、版本化镜像和固定镜像摘要/版本标签。
- [x] 提供 `install`、`upgrade`、`backup`、`restore`、`rollback` 和 `status` 命令。
- [x] 默认仅开放 80/443；MySQL、Redis 和业务端口留在私有网络。
- [x] 支持域名、Let's Encrypt TLS、HSTS 和安全响应头。
- [x] 实现数据库每日备份、媒体备份、保留策略、加密和恢复演练。
- [x] 增加结构化日志、健康状态、CPU/内存/磁盘、接口错误率和 AI 成本监控。
- [x] 增加登录、上传、搜索、下单和 AI 的分级限流。
- [x] 增加依赖、镜像和密钥泄漏扫描；生产日志脱敏。
- [x] 编写升级兼容矩阵、迁移前检查和失败回滚说明。

验收门槛：

- 新实例可在目标 Linux 服务器一条命令安装并通过健康检查。
- 数据库和媒体备份可在空机器恢复，恢复结果经过业务抽查。
- 任一应用容器退出后可自动恢复；依赖不可用时有明确告警。
- 外部只能访问 HTTPS 商城和必要的公开资源。

2026-07-15 实机验收记录：一键升级、健康检查、加密备份与隔离恢复、版本回滚、容器异常退出自动恢复、Prometheus 告警 firing/resolved 闭环、Alertmanager 接收、8 个采集目标以及四服务 ECS JSON 日志均已通过。生产 TLS 配置、证书签发和安全响应头已有自动化校验；由于当前环境尚未提供真实域名和公网 DNS，最后一项“公网只能访问 HTTPS”仍需在域名就绪后完成现场验收，不能以本地自签名证书替代。

### 阶段 7：质量体系、试点与发布

**目标**：通过真实商家试点验证产品，而不是只验证技术启动。

自动化测试任务：

- [x] 后端：领域单元测试、权限测试、Testcontainers 集成测试和迁移测试。
- [x] 前端：Vitest 组件测试、API Mock 测试和类型检查。
- [x] 端到端：Playwright 覆盖店主上架、消费者下单、店主发货和消费者查看物流。
- [x] 并发：库存 100、并发购买大于 100 时成功数不超过 100，且无负库存。
- [x] 故障：Redis/MySQL 短暂不可用、固定上游中断、服务重启和重复回调场景。
- [x] 安全：越权、上传、XSS、暴力登录、Token 和敏感信息检查。

试点任务：

- [x] 使用非生产数据进行内部验收。
- [ ] 选择 1 家试点商家，导入少量真实商品并运行完整履约流程。
- [ ] 记录商家操作耗时、失败点、支持请求和消费者反馈。
- [ ] 修复阻塞性问题后再扩大到更多商家。
- [x] 建立版本发布说明、已知问题和升级窗口。

发布门槛：

- 所有 P0/P1 缺陷关闭，关键 E2E 连续通过。
- 备份恢复和版本回滚至少完成一次真实演练。
- 商家能够在无开发人员介入的情况下完成上架和发货。
- 试点期间无丢单、越权、负库存和不可恢复的数据错误。

2026-07-15 自动化与内部技术验收已完成，证据汇总见
[`质量与安全验收记录.md`](质量与安全验收记录.md)。真实商家试点、现场反馈和公网域名验收仍未完成，
执行步骤见 [`试点验收手册.md`](试点验收手册.md)，不得把自动化结果替代为真实商家试点结论。

## 6. 真实支付的独立阶段

真实支付属于高风险外部集成，不阻塞 `COD/OFFLINE` 首版，但正式宣称“在线商城”前必须单独实施。

任务：

- [ ] 选择微信支付、支付宝或合适的聚合支付服务，并确认商家资质要求。
- [ ] 支付订单与业务订单分表，保存渠道流水号、金额、状态和回调摘要。
- [ ] 验签、金额核对、幂等回调、主动查单、超时关单和对账。
- [ ] 支付成功仅由可信渠道回调或主动查单确认，消费者不能直接标记支付成功。
- [ ] 设计退款申请、退款执行、失败重试和库存/订单状态联动。
- [ ] 使用渠道沙箱完成自动化测试，再由商家提供正式凭据上线。

验收门槛：

- 重复、乱序、伪造和延迟回调不会改变错误订单。
- 订单金额与支付金额不一致时拒绝确认并告警。
- 每日对账能识别单边账，退款状态可追踪。

## 7. API 与页面规划

### 7.1 商家 API 建议

```text
POST   /api/merchant/bootstrap
POST   /api/merchant/login
GET    /api/merchant/store
PUT    /api/merchant/store

GET    /api/merchant/products
POST   /api/merchant/products
GET    /api/merchant/products/{id}
PUT    /api/merchant/products/{id}
POST   /api/merchant/products/{id}/publish
POST   /api/merchant/products/{id}/unpublish

POST   /api/merchant/products/{id}/skus
PUT    /api/merchant/skus/{skuId}
POST   /api/merchant/skus/{skuId}/stock-adjustments
GET    /api/merchant/stock-ledger

GET    /api/merchant/orders
GET    /api/merchant/orders/{orderNo}
PUT    /api/merchant/orders/{orderNo}/note
POST   /api/merchant/orders/{orderNo}/confirm-payment
POST   /api/merchant/orders/{orderNo}/accept
POST   /api/merchant/orders/{orderNo}/ship
POST   /api/merchant/orders/{orderNo}/close

GET    /api/merchant/ai/settings
PUT    /api/merchant/ai/settings
GET    /api/merchant/ai/index-tasks
POST   /api/merchant/ai/index-tasks/{id}/retry
```

具体 URI 可在实现时微调，但必须保持消费者 API 与商家 API 的权限边界清晰。

### 7.2 商家页面建议

```text
/merchant/login
/merchant/dashboard
/merchant/store
/merchant/products
/merchant/products/new
/merchant/products/:id/edit
/merchant/inventory
/merchant/stock-ledger
/merchant/orders
/merchant/orders/:orderNo
/merchant/staff
/merchant/ai
/merchant/system
```

## 8. 优先级与建议里程碑

| 里程碑 | 包含阶段 | 能力结果 | 建议发布标识 |
| --- | --- | --- | --- |
| M0 开发基线 | 阶段 0 | 可重复开发、测试和部署 | `0.4.0-dev` |
| M1 商家可配置 | 阶段 1 | 店主可登录并配置店铺 | `0.5.0-alpha` |
| M2 商家可上架 | 阶段 2 | 商品、SKU、图片、库存闭环 | `0.6.0-alpha` |
| M3 商家可履约 | 阶段 3-4 | 消费者下单，商家发货 | `0.8.0-beta` |
| M4 AI 可运营 | 阶段 5 | AI 与真实商品同步且可控 | `0.9.0-beta` |
| M5 试点候选 | 阶段 6-7 | 可安装、备份、监控和试点 | `1.0.0-rc.1` |
| M6 正式发布 | 试点缺陷关闭 | 单商家生产可用 | `1.0.0` |

单人开发应优先完成 M3，再决定 AI 深化和真实支付。不要同时展开所有阶段。时间估算应在每个阶段拆分 Issue 后根据开发者投入重新计算，不把日期承诺写死在路线图中。

## 9. Issue 拆分与执行规则

每个阶段按以下模板拆分 GitHub Issue：

```markdown
## 背景
为什么需要这个能力，解决哪个商家或消费者问题。

## 范围
- 包含什么
- 明确不包含什么

## 设计
- 数据模型/迁移
- API 契约
- 页面与交互
- 权限与安全
- 失败与回滚路径

## 验收标准
- Given / When / Then 场景
- 自动化测试
- 手工验收步骤

## 发布影响
- 环境变量
- 数据迁移
- 兼容性
- 回滚方案
```

执行约束：

- 一个 PR 只解决一个清晰目标，避免同时重构无关模块。
- 数据库迁移一经合并不得修改已成功执行的版本，修复必须新增迁移。
- 新写接口必须同时提交权限测试、异常测试和接口文档。
- 新页面必须包含加载、空数据、失败、无权限和移动端状态。
- 任何涉及订单、库存、支付的改动必须说明幂等和回滚路径。
- PR 合并前必须通过 CI；部署后执行冒烟测试并保留版本与迁移记录。

## 10. Definition of Done

一个功能只有同时满足以下条件才算完成：

- [ ] 需求范围和非目标清楚。
- [ ] 数据迁移可从旧版本升级，并说明备份/回滚方式。
- [ ] 后端权限、校验、幂等和错误响应完整。
- [ ] 前端包含正常、加载、空、失败和无权限状态。
- [ ] 单元、集成或端到端测试覆盖主要风险。
- [ ] API、环境变量、部署和用户文档已更新。
- [ ] 不记录或泄露密码、Token、支付凭据和不必要的个人信息。
- [ ] CI 通过，目标服务器冒烟测试通过。
- [ ] 验收标准由实际结果证明，不以“代码已写完”代替。

## 11. 第一批建议 Issue

建议下一轮开发按以下顺序创建和执行：

1. `M0-01`：统一应用 Docker Compose、健康检查和可配置网关端口。
2. `M0-02`：建立后端集成测试、前端测试与核心冒烟测试骨架。
3. `M0-03`：编写单店架构、SKU、权限和支付策略 ADR。
4. `M1-01`：增加用户角色、状态和一次性店主初始化流程。
5. `M1-02`：实现 `/api/merchant/**` 网关与服务层权限。
6. `M1-03`：实现店铺配置模型、接口和消费者动态品牌展示。
7. `M1-04`：实现商家登录、布局、路由守卫和店铺设置页面。
8. `M2-01`：设计并迁移 Product/SKU/Stock 数据模型。
9. `M2-02`：实现商家商品、SKU 和上下架 API。
10. `M2-03`：实现商品与库存管理页面及库存流水。

完成上述十项后再进入订单履约开发，可以显著降低边做后台边修改底层商品模型造成的返工。

## 12. 最终验收场景

`1.0.0` 发布前必须完整演示并留存测试结果：

1. 在空服务器安装系统并创建唯一店主。
2. 店主设置店名、Logo、联系方式和配送说明。
3. 店主创建一个包含多个 SKU 和图片的商品，录入库存并上架。
4. 消费者在桌面或手机端找到商品，通过 AI 获得基于真实数据的推荐。
5. 消费者注册、选择 SKU、加入购物车并使用线下付款或货到付款下单。
6. 商家在工作台查看订单、确认处理、填写承运商和运单号并发货。
7. 消费者查看物流信息并确认完成。
8. 取消或超时订单正确释放库存，重复操作不破坏状态。
9. 商品下架后消费者和 AI 均不再推荐或允许购买。
10. 执行备份，在另一套空环境恢复，并核对商品、订单、库存和媒体。

只有这一场景可稳定、可重复地完成，项目才从“可演示商城”进入“可供单个商家试点使用”的状态。
